{
  "name": "tasklet",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny disposable web app used as the acceptance fixture for the coding-agent execution plane.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  }
}
